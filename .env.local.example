# Local development environment variables
# NEVER COMMIT THIS FILE - Always keep in .gitignore

# Server Configuration
SERVER_HOST=your_server_ip
SERVER_USER=your_username
SERVER_PATH=/path/to/your/site

# SSH Configuration (backup - prefer using SSH keys)
# SERVER_PASSWORD=your_new_secure_password

# FTP Configuration (if needed)
FTP_HOST=your_server_ip
FTP_USER=your_username
# FTP_PASSWORD=your_new_secure_password

# Database (if needed for local development)
DB_HOST=your_server_ip
DB_DATABASE=your_database_name
DB_USERNAME=your_db_username
# DB_PASSWORD=your_new_secure_password

# SECURITY NOTES:
# 1. Use SSH keys instead of passwords when possible
# 2. If you must use passwords, make them strong and unique
# 3. Never commit this file to git
# 4. Rotate passwords regularly